# Optional path overrides. No Strava credentials are required.
ENDURANCE_DB_PATH=data/endurance_lab.sqlite3
ENDURANCE_IMPORT_DIR=data/import
ENDURANCE_RAW_DIR=data/raw
ENDURANCE_ATHLETE_CONFIG=config/athlete.yaml

# Set only when serving behind a trusted private reverse proxy.
ENDURANCE_BIND_ADDRESS=127.0.0.1
